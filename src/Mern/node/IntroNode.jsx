import React from "react";

function IntroNode() {
  return (
    <div className="p-3 bg-light">
      <div>
        <h2>Node.js Tutorial</h2>
        <p>
          node module used to provide information about the computer operating
          system. Advantages: It provides functions to interact with the
          operating system. It provides the Node allows developers to write
          JavaScript code that runs directly in a computer process itself
          instead of in a browser. Node can, therefore, be used to write
          server-side applications with access to the operating system, file
          system, and everything else required to build fully-functional
          applications. Node. hostname of the operating system and returns the
          amount of free system memory in bytes
        </p>
        <p>
          Node.js is a cross-platform environment and library for running
          JavaScript applications, Node allows developers to write JavaScript
          code that runs directly in a computer process itself instead of in a
          browser. Node can, therefore, be used to write server-side
          applications with access to the operating system, file system, and
          everything else required to build fully-functional applications. Node.
          which is used to create networking and server-side applications.
        </p>
        <p>
          Our Node.js tutorial includes all topics of Node.js, such as Node.js
          installation on Windows and Linux, REPL, package manager, callbacks,
          event loop, OS, path, query string, cryptography, debugger, URL, DNS,
          Net, UDP, process, child processes, buffers, streams, file systems,
          global objects, web modules, etc. There are also given Node.js
          interview questions to help you better understand the Node.js
          technology.
        </p>
      </div>
    </div>
  );
}

export default IntroNode;
